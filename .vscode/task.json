{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Push on save",
            "type": "shell",
            "command": "git add . && git commit -m 'automating push' && git push",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}